## Packages
framer-motion | Page transitions and animations
date-fns | Date formatting for logs
recharts | Dashboard charts and stats
@hookform/resolvers | Form validation integration

## Notes
- Tailwind config needs to extend font families with `--font-sans` and `--font-display`.
- Application assumes `api` and `buildUrl` are correctly exported from `@shared/routes`.
- Settings are expected to exist (1 row in the settings table). 
